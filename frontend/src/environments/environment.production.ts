/**
 * Configuración para `ng build` (producción).
 * Cambia `apiBase` por la URL pública del backend antes de desplegar
 * (Render / Railway / Fly.io), p. ej. https://agroia-backend.onrender.com
 */
export const environment = {
  produccion: true,
  apiBase: 'https://tu-backend-en-produccion.com',
};
